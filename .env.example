# Database (SQLite - just a file path)
DATABASE_URL=file:./data/tennis.db

# Cron job protection
CRON_SECRET=cron-secret

# Telegram Bot
TELEGRAM_BOT_TOKEN=123456:ABC-xxxxx

# Email via Resend (HTTP API - works on Railway/cloud)
# Sign up at https://resend.com (free: 3000 emails/month)
# IMPORTANT: EMAIL_FROM must use a domain verified in your Resend account
# If you haven't verified a domain, Resend will reject emails
# Example: EMAIL_FROM='Your App Name <noreply@yourdomain.com>'
RESEND_API_KEY=re_xxxxxxxxxxxx
EMAIL_FROM=Your App Name <noreply@yourdomain.com>

# App URL (for links in notifications)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Auth (NextAuth v5)
AUTH_SECRET=some-random-string
# AUTH_URL is required in production - set to your app's public URL
# AUTH_URL=https://your-app.railway.app

# --- Phase 3 feed-primary ingestion (3-clock hybrid) ---
# Cutover switch. When true the cron runs the feed-primary ingestion
# (Clock 1 head-poll + Clock 2b watch-reconcile + Clock 3 daily sweep) INSTEAD of
# the blind HTML scrape. Default off keeps the legacy scraper (no regression).
# Prereq before enabling: `courts` populated + `slots` reset to feed-owned rows
# (see docs/HANDOFF.md cutover procedure).
# FEED_INGEST_ENABLED=false
# Clock 2b: max HTML pages (venue-days) per reconcile run — bounds cost.
# RECONCILE_MAX_PAGES=40
# Clock 2b throttle (minutes) — the notification-miss SLA for feed stale misses.
# RECONCILE_INTERVAL_MIN=15
# Clock 3 throttle (hours) between full sweeps.
# SWEEP_INTERVAL_HOURS=24
# How often (hours) to refresh venue/court metadata from the facility feed.
# FACILITY_REFRESH_HOURS=6