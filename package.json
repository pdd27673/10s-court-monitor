{
  "name": "10s-court-monitor",
  "version": "1.0.0",
  "description": "Court booking availability monitoring system with notifications",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces",
    "build": "npm run build -w @10s/database && npm run build -w web",
    "build:scraper": "npm run build -w @10s/database",
    "start": "npm run start --workspaces",
    "start:scraper": "cd apps/scraper && npm start",
    "lint": "npm run lint --workspaces",
    "clean": "rm -rf node_modules && rm -rf apps/*/node_modules && rm -rf packages/*/node_modules"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
