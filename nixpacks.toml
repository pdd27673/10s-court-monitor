# Nixpacks configuration for Railway.
# Runtime is Node 24 + Postgres (pg). No native addon compile toolchain.

[phases.setup]
nixPkgs = ["nodejs_24"]

[phases.install]
cmds = ["npm ci --include=dev"]

[phases.build]
cmds = ["npm run build"]

# db:migrate runs at deploy time (railway.json startCommand), where DATABASE_URL
# is available — not at build time.
[start]
cmd = "npm run db:migrate && npm start"
